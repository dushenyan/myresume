# File Wizard 项目文档

> 基于 Git 提交历史生成的项目全面分析报告
> 生成日期：2026-04-02

---

## 📋 目录

- [项目概况](#项目概况)
- [项目亮点](#项目亮点)
- [已解决和优化的问题](#已解决和优化的问题)
- [主要贡献者](#主要贡献者)
- [Git 历史查询命令](#git-历史查询命令)

---

## 📊 项目概况

### 基本信息

| 项目属性 | 详情 |
|---------|------|
| **项目名称** | file-wizard（文件魔法师） |
| **版本** | 0.1.0 |
| **描述** | 文件类型整理、分类规则和 AI 功能的 CLI 工具 |
| **作者** | dushenyan <dushenyan88@gmail.com> |
| **仓库地址** | https://github.com/dushenyan/file-wizard |
| **开发语言** | TypeScript |
| **运行环境** | Node.js >= 18.17 |
| **项目周期** | 2026-01-18 至 2026-03-17 |

### 技术栈

**核心依赖：**
- `commander` - CLI 命令行框架
- `@clack/prompts` - 交互式命令行界面
- `chalk` - 终端字符串样式
- `fs-extra` - 文件系统扩展
- `yaml` - YAML 配置解析
- `zod` - 数据验证

**开发工具：**
- `tsdown` - TypeScript 打包工具
- `vitest` - 单元测试框架
- `eslint` - 代码质量检查
- `typescript` - TypeScript 编译器

### 项目结构

```
file-wizard/
├── src/
│   ├── clients/          # AI 提供商客户端实现
│   ├── commands/         # CLI 命令处理逻辑
│   ├── interactive/      # 交互式 UI 模式
│   ├── mcp/             # MCP 协议集成
│   ├── utils/           # 工具函数
│   ├── ai-file-manager.ts  # AI 文件管理核心
│   ├── cli.ts           # CLI 入口
│   └── config.ts        # 配置管理
├── .agents/skills/      # 技能配置
└── 配置文件
```

### 代码统计

| 指标 | 数值 |
|------|------|
| **总提交次数** | 28 次 |
| **代码新增** | 30,875 行 |
| **代码删除** | 3,224 行 |
| **净增代码** | 27,651 行 |

---

## ✨ 项目亮点

### 1. 多 AI 提供商支持

项目支持 6 种主流 AI 提供商，为用户提供灵活的选择：

- **Groq**（推荐）- 免费层，30 requests/minute
- **Gemini** - Google AI 服务
- **DeepSeek** - 国产 AI 服务
- **Qwen** - 阿里云通义千问
- **OpenRouter** - AI 模型聚合平台
- **智谱 AI** - 清华技术支持

**技术实现：**
- 统一的客户端接口设计
- 基类的抽象与复用
- 配置化的模型参数管理

### 2. 智能文件整理

**核心功能：**
- 按扩展名、日期、大小、名称分类文件
- AI 驱动的智能分类和重命名
- 批量处理优化，减少 AI 调用次数
- 支持自定义提示词和命名标准

**创新点：**
- 结合文件内容分析进行智能分类
- 支持中文文件夹和文件名
- Dry-run 模式确保操作安全

### 3. MCP（Model Context Protocol）集成

**功能特性：**
- 标准化的 AI 模型通信协议
- 文件元数据自动提取
- 工具调用能力扩展
- 支持文件内容读取和分析

**技术优势：**
- 解耦 AI 提供商与业务逻辑
- 统一的文件操作接口
- 可扩展的工具生态

### 4. 交互式 UI 模式

**用户体验：**
- 基于 `@clack/prompts` 的现代化交互界面
- 键盘导航和选择
- 实时进度反馈
- 彩色输出和渐变效果

**设计理念：**
- 降低使用门槛
- 提供直观的操作引导
- 支持命令行和交互式两种模式

### 5. 完善的代理支持

**网络适配：**
- 自动检测系统代理配置
- 支持手动配置代理地址
- 确保 AI 服务访问顺畅
- 详细的代理诊断工具

### 6. 灵活的配置系统

**配置管理：**
- YAML 格式配置文件
- 多提供商配置支持
- 环境变量覆盖
- 配置验证和错误提示

---

## 🔧 已解决和优化的问题

### CLI 功能优化

#### 1. 帮助选项检测逻辑改进
**问题：** 早期版本的帮助选项检测不够准确，可能导致帮助信息显示异常。

**解决方案：**
- 重构帮助选项的检测逻辑
- 添加自定义帮助选项处理
- 改进输出格式和对齐宽度

**影响：** 提升了 CLI 的用户体验和命令可读性。

#### 2. 版本显示功能完善
**问题：** 初始版本缺少版本信息显示，用户无法快速查看工具版本。

**解决方案：**
- 添加 `-V, --version` 选项
- 实现版本号自动读取
- 优化版本信息输出格式

**影响：** 方便用户和开发者确认工具版本。

### 代码架构优化

#### 3. 代码结构重构
**问题：** 早期代码结构较为简单，随着功能增加需要更好的组织方式。

**解决方案：**
- 分离命令处理和交互式逻辑
- 提取 MCP 客户端初始化到独立模块
- 统一 AI 客户端接口设计

**影响：** 提高了代码的可维护性和可扩展性。

#### 4. 文件选择器优化
**问题：** 文件选择器缺少灵活的排除机制，无法满足复杂场景需求。

**解决方案：**
- 添加配置文件支持排除模式
- 增强排除模式匹配逻辑
- 支持目录选择功能

**影响：** 提升了文件处理的灵活性和准确性。

### 功能增强

#### 5. 日志系统集成
**问题：** 缺少统一的日志管理，调试和问题排查困难。

**解决方案：**
- 实现统一的日志工具类
- 集成到 MCP 客户端
- 支持 verbose 模式输出详细信息

**影响：** 提升了开发调试效率和问题定位能力。

#### 6. 自定义提示词功能
**问题：** 固定的提示词无法满足不同用户的命名风格需求。

**解决方案：**
- 添加自定义提示词选项
- 统一使用自定义提示词替代固定选项
- 支持目录级别的提示词配置

**影响：** 提升了工具的灵活性和适用范围。

### 性能优化

#### 7. AI 调用优化
**问题：** 批量处理文件时 AI 调用次数过多，影响效率。

**解决方案：**
- 实现批量处理优化
- 减少不必要的 AI 调用
- 优化提示词以获得更准确的结果

**影响：** 提升了处理效率，降低了 API 调用成本。

---

## 👥 主要贡献者

### Elegant

**贡献概览：**
- **提交次数：** 21 次
- **代码变更：** 新增 12,818 行，删除 1,885 行
- **贡献周期：** 2026-01-18 至 2026-01-23
- **主要角色：** 项目创始人、核心架构师

**主要贡献：**

#### 项目初始化与基础架构
- 🎯 初始化文件管理助手项目
- 🏗️ 重构文件管理功能并添加 AI 智能整理支持
- 📦 建立项目基础结构和配置体系

#### 核心功能开发
- 🤖 集成 MCP 客户端并增加文件管理功能
- 🎨 添加交互式 UI 模式并增强文件管理功能
- 🔧 整合重命名和分类功能到智能管理命令
- 📝 重构代码结构并添加交互式命令功能

#### AI 提供商集成
- 🌐 添加 OpenRouter 支持并更新 Qwen 接口
- 📊 添加文件内容分析功能并支持 Qwen 提供商
- 🔑 实现多 AI 提供商统一接口

#### CLI 功能完善
- 💻 添加版本显示功能并改进帮助信息输出
- 📋 为子命令添加自定义帮助选项处理和输出格式
- 🎨 调整控制台输出样式和顺序
- 🔍 添加 verbose 选项以输出详细信息

#### 工具与基础设施
- 📦 添加 bumpp 依赖并配置发布脚本
- 🛠️ 添加忽略列表功能支持
- 🐛 改进帮助选项的检测逻辑
- 📝 添加日志工具并集成到 MCP 客户端

**贡献特点：**
- 注重架构设计和代码质量
- 关注用户体验和交互设计
- 推动项目从概念到完整产品的演进

---

### dushenyan

**贡献概览：**
- **提交次数：** 7 次
- **代码变更：** 新增约 18,057 行，删除约 1,339 行
- **贡献周期：** 2026-03-16 至 2026-03-17
- **主要角色：** 功能开发者、优化专家

**主要贡献：**

#### AI 提供商扩展
- 🤖 添加智谱 AI 客户端支持
- 扩展 AI 服务生态，提供更多选择

#### 文件选择器增强
- 📁 添加配置文件支持排除模式
- 🔧 增强排除模式匹配逻辑并移除调试日志
- 🎯 优化文件选择器功能

#### CLI 功能增强
- 💡 添加自定义提示词选项并支持目录选择
- 提升工具的灵活性和适用性

#### 代码优化
- 🔨 提取 MCP 客户端初始化逻辑到独立模块
- 提高代码的可维护性和复用性

#### 文档与技能
- 📚 添加技能链接文件及文档
- 完善项目文档体系

**贡献特点：**
- 专注于功能扩展和性能优化
- 注重代码质量和可维护性
- 持续改进用户体验

---

### 其他指定提交者

#### 杜审言
**状态：** 未在 Git 提交历史中找到相关记录

**可能原因：**
- 该名称可能为别名或昵称
- 可能使用了不同的 Git 配置
- 可能参与了非代码贡献（如文档编写、测试等）

#### shenyandu
**状态：** 未在 Git 提交历史中找到相关记录

**可能原因：**
- 该名称可能是邮箱用户名的一部分
- 可能使用了不同的 Git 配置
- 可能参与了其他形式的贡献

#### 十七
**状态：** 未在 Git 提交历史中找到相关记录

**可能原因：**
- 该名称可能为昵称或代号
- 可能使用了不同的 Git 配置
- 可能参与了非代码贡献

---

## 🔍 Git 历史查询命令

以下命令供您查询详细的提交历史记录：

### 查看完整提交历史

```bash
# 查看所有提交的详细信息
git log --all --pretty=format:'%H|%an|%ae|%ad|%s' --date=iso

# 查看简洁的提交历史
git log --all --oneline --graph

# 查看最近 N 次提交
git log -n 10 --oneline
```

### 按提交者查询

```bash
# 查看 Elegant 的所有提交
git log --all --author="Elegant" --pretty=format:'%ad|%s' --date=short

# 查看 dushenyan 的所有提交
git log --all --author="dushenyan" --pretty=format:'%ad|%s' --date=short

# 查看指定邮箱的所有提交
git log --all --author="63347504+dushenyan@users.noreply.github.com" --oneline

# 查看所有提交者列表
git log --all --format='%aN' | sort -u
```

### 统计分析命令

```bash
# 查看提交次数统计
git shortlog -sn --all

# 查看代码变更统计
git log --all --numstat --pretty=format:'' | awk 'NF==3 {add+=$1; del+=$2} END {printf "Added: %d lines, Deleted: %d lines\n", add, del}'

# 查看指定作者的代码变更统计
git log --all --author="Elegant" --numstat --pretty=format:'' | awk 'NF==3 {add+=$1; del+=$2} END {printf "Added: %d lines, Deleted: %d lines\n", add, del}'

# 查看每个提交者的详细统计
git log --all --format='%aN' | sort -u | while read name; do
  echo -en "$name\t"
  git log --all --author="$name" --pretty=tformat: --numstat | awk '{add+=$1; del+=$2} END {printf "Added: %d, Deleted: %d\n", add, del}'
done
```

### 按时间范围查询

```bash
# 查看指定日期范围的提交
git log --all --since="2026-01-01" --until="2026-03-31" --oneline

# 查看某个月的提交
git log --all --since="2026-01-01" --until="2026-01-31" --oneline

# 查看最近一周的提交
git log --all --since="1 week ago" --oneline
```

### 按文件或目录查询

```bash
# 查看某个文件的修改历史
git log --all --follow -- <文件路径>

# 查看某个目录的所有提交
git log --all -- <目录路径>

# 查看某个文件的所有修改者
git log --all --format='%aN' -- <文件路径> | sort -u
```

### 提交内容分析

```bash
# 查看每次提交的文件变更统计
git log --all --stat --oneline

# 查看某次提交的详细内容
git show <commit-hash>

# 查看某次提交的文件列表
git show --name-only <commit-hash>
```

### 按提交类型查询

```bash
# 查看所有功能添加提交
git log --all --grep='feat' --oneline

# 查看所有问题修复提交
git log --all --grep='fix' --oneline

# 查看所有重构提交
git log --all --grep='refactor' --oneline

# 查看所有文档更新提交
git log --all --grep='docs' --oneline
```

### 生成提交报告

```bash
# 生成简洁的提交报告
git log --all --pretty=format:'%h - %an, %ar : %s' --date=relative

# 生成详细的提交报告（包含文件统计）
git log --all --pretty=format:'%H%n作者: %an <%ae>%n日期: %ad%n说明: %s%n' --date=iso --stat

# 导出提交历史到文件
git log --all --pretty=format:'%H|%an|%ae|%ad|%s' --date=iso > commit-history.csv
```

---

## 📈 项目发展趋势

### 开发时间线

```
2026-01-18  项目初始化
    ↓
2026-01-19  交互式UI和智能管理功能
    ↓
2026-01-20  CLI功能完善和发布配置
    ↓
2026-01-21  AI提供商集成
    ↓
2026-01-22  MCP协议集成
    ↓
2026-01-23  日志系统和代码优化
    ↓
    [开发间歇期]
    ↓
2026-03-16  智谱AI支持和文件选择器优化
    ↓
2026-03-17  新技能添加和功能优化
```

### 版本演进

- **v0.1.0** - 当前版本，包含核心功能
- 持续迭代中，功能不断完善

### 未来规划

根据 README 中的 TODO 列表：
- [ ] 支持更多文件分类规则
- [ ] 添加文件内容分析功能
- [ ] 增加更多 AI 提供商支持
- [ ] 适配中文文档

---

## 🎯 总结

File Wizard 是一个功能完善、架构清晰的 AI 驱动文件管理 CLI 工具。项目在短短两个月内完成了从初始化到功能完善的演进，展现了高效的开发节奏和良好的代码质量。

**核心优势：**
- 多 AI 提供商支持，灵活性高
- 智能文件整理，提升效率
- MCP 协议集成，扩展性强
- 交互式 UI，用户体验佳
- 完善的配置系统，易于定制

**团队协作：**
- Elegant 主导项目架构和核心功能开发
- dushenyan 负责功能扩展和优化
- 两人协同配合，推动项目快速发展

项目代码质量高，架构设计合理，具有良好的可维护性和可扩展性，为后续功能迭代奠定了坚实基础。

---

*本文档基于 Git 提交历史自动生成，数据截止至 2026-03-17*

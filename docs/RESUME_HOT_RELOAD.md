# 简历热更新使用说明

## 功能介绍

现在支持在修改 `src/resumeSource.ts` 文件时自动更新 `resume/resume.json` 文件，无需手动重新编译。

## 使用方法

### 1. 命令行方式

#### 单次生成简历
```bash
npm run resume:generate
```

#### 开发模式（推荐）
启动监听模式，自动检测 `resumeSource.ts` 文件变化：
```bash
npm run resume:watch
```

#### 完整开发环境
同时启动简历监听和开发服务器：
```bash
npm run resume:dev
```

### 2. 编程方式使用

```typescript
import { ResumeGenerator } from './src/generateResume'

// 方法1: 监听 resumeSource.ts 文件变化
ResumeGenerator.watchResumeSource('./resume/resume.json')

// 方法2: 手动从 resumeSource 生成
ResumeGenerator.generateFromSource('./resume/resume.json')

// 方法3: 停止监听
ResumeGenerator.stopWatching()
```

## 热更新机制

1. **文件监听**: 使用 `fs.watchFile` 监听 `src/resumeSource.ts` 文件变化
2. **模块缓存清理**: 检测到文件变化时自动清除模块缓存
3. **数据验证**: 重新生成前验证简历数据完整性
4. **自动导出**: 验证通过后自动导出到指定路径

## 集成说明

- **Grunt 集成**: 开发模式 (`npm run dev`) 已包含简历文件监听
- **实时反馈**: 文件变化时会在控制台显示更新状态
- **错误处理**: 生成失败时会显示详细错误信息
- **优雅退出**: 使用 Ctrl+C 可安全停止监听

## 注意事项

1. 修改 `resumeSource.ts` 文件后会自动触发重新生成
2. 如果数据验证失败，会显示错误信息但不会中断监听
3. 监听模式下请保持终端开启以获得实时更新
4. 建议使用 `npm run resume:dev` 同时启动开发服务器和简历监听

## 示例输出

```
🔄 检测到 resumeSource.ts 文件更新，重新生成简历...
✅ 数据验证通过!
简历数据已成功导出到: ./resume/resume.json
✅ 简历更新完成，继续监听文件变化...
```